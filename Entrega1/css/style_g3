@charset "utf-8";

@import url('https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet'); /* Importa la fuente desde Google Fonts */

* {
    font-family: 'Noto Sans', sans-serif;
}

/* Establece propiedades generales del documento */
html, body {
    background-color: rgb(0, 0, 0, 0);
    color: rgb(176, 119, 35);
    font-family: 'Noto Sans', sans-serif;
    margin: 0;
    padding: 0;
    text-align: center;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    height: 100%;
} 

body {
    padding-top: 80px; /* Añade espacio en la parte superior para que el contenido no quede oculto por el header */
}

/* Estilos del encabezado */
header {
    display: flex; /* Usa flexbox para organizar los elementos */
    justify-content: space-between; /* Distribuye los elementos a los extremos */
    align-items: center; /* Alinea los elementos verticalmente */
    padding: 15px;
    background: linear-gradient(180deg, rgba(255, 152, 0, 1), rgba(255, 87, 34, 1)); /* Degradado naranja */
    position: fixed; /* Fija el encabezado en la parte superior */
    width: 100%;
    top: 0;
    left: 0;
    z-index: 1000; /* Asegura que esté por encima de otros elementos */
}
 
/* Ajusta la imagen dentro del header */
header img {
    height: 60px;
}

/* Estilos de la barra de navegación */
nav {
    margin-right: 2%;
}
nav a {
    color: rgb(0, 0, 0);
    text-decoration: none; /* Elimina el subrayado de los enlaces */
    margin: 0 15px;
    font-weight: bold;
}

/* Estilos de los subtítulos */
h2 {
    margin-top: 5%;
    font-size: 1.6em;
    color: #ff5722; /* Color naranja */
}

/* Contenedor del logo */
.logo_container { 
    position: center;
    font-size: 3.3em; 
}

.logo_container span { 
    position: absolute; 
    top: 30%; 
}

/* Estilos del banner */
.banner {
    background-color: rgb(0, 0, 0);
    height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 30px;
    font-weight: bold;
    flex-shrink: 0;
}

/* Contenedor de categorías */
.categorias {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    padding: 2% 0;
}

/* Estilos de cada categoría */
.categorias > div {
    margin: 15px;
    width: 200px;
    border-radius: 20px;
    overflow: hidden;
    cursor: pointer; /* Cambia el cursor al pasar por encima */
}
.categorias > div img {
    display: block;
    margin: 0 auto;
    width: auto;
    max-width: 250px;
    border-radius: 20px;
    transition: filter 0.3s ease-in-out, transform 0.3s ease-in-out; /* Agrega transición en hover */
}

/* Efecto hover: escala de grises y zoom */
.categorias > div:hover img {
    filter: grayscale(100%);
    transform: scale(1.1);
}
.categorias h3 { 
    padding: 10px;
    margin: 0;
    font-size: 1em;
}

/* Estilos del pie de página */
footer {
    background-color: #222; /* Fondo oscuro */
    color: #bbb; /* Texto en gris claro */
    text-align: center;
    padding: 15px;
    margin-top: auto;
    margin-bottom: 0px;
} 

/* Contenedor del logo dentro del footer */
.logo_container { 
    align-items: center;
    gap: 10px; /* Espaciado entre el logo y el texto */
}

.logo_container img {
    max-width: 100px; /* Ajuste de tamaño máximo del logo */
    height: auto;
}

.logo_container span {
    font-size: 1.5em;
    white-space: nowrap;
}

/* Estilos responsivos para pantallas pequeñas */
@media screen and (max-width: 768px) {
    .logo_container {
        flex-direction: column;
        text-align: center;
    }
    
    .logo_container img {
        max-width: 80px; /* Reduce el tamaño del logo en dispositivos pequeños */
    }

    .logo_container span {
        font-size: 1.2em;
    }

}



